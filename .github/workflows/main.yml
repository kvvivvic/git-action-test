name: React build
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-lastst
    steps:
      - name: checkout source code.
        uses: actions/checkout@main

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build
